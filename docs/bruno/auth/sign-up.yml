info:
  name: sign-up
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/auth/sign-up"
  body:
    type: json
    data: |-
      {
        "email": "user@email.com",
        "password": "Aa123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
