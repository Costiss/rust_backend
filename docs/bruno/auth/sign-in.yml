info:
  name: sign-in
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/auth/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "user@email.com",
        "password": "Aa123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
